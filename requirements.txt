# Run  python .dev/init.py  to generate the final requirements.txt
# for your chosen auth method.
#
# Base dependencies (always needed):
Flask==3.1.1
Flask-Limiter==3.12
python-dotenv==1.1.0
SQLAlchemy==2.0.41
psycopg2-binary==2.9.10
argon2-cffi==25.1.0
gunicorn==20.1.0
